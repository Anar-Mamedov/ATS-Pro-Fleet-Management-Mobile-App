<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Base configuration for all connections -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <!-- Trust system (Android) certificate store -->
      <certificates src="system" />
      <!-- Trust user-installed certificates (for debugging with proxies) -->
      <certificates src="user" />
    </trust-anchors>
  </base-config>

  <!-- Domain-specific configuration for production API -->
  <domain-config cleartextTrafficPermitted="false">
    <domain includeSubdomains="true">api.atspro.com.tr</domain>
    <trust-anchors>
      <certificates src="system" />
      <!-- Sectigo intermediate CA for Xiaomi/Oppo compatibility -->
      <certificates src="@raw/sectigo_intermediate" />
    </trust-anchors>
  </domain-config>

  <!-- Optional: Allow cleartext for localhost/development (if needed) -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">localhost</domain>
    <domain includeSubdomains="true">10.0.2.2</domain>
    <domain includeSubdomains="true">127.0.0.1</domain>
  </domain-config>
</network-security-config>
